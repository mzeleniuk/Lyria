<div class="container m-t-lg">
  <div class="row">
    <div class="col-lg-6 col-lg-offset-3 col-md-6 col-md-offset-3 col-sm-6 col-sm-offset-3 col-xs-12">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title text-center">
            Edit <%= resource_name.to_s.humanize %>
          </h3>
        </div>

        <div class="panel-body">
          <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {method: :put}) do |f| %>
            <% if devise_error_messages!.present? %>
              <div class="alert alert-danger m-b-md" role="alert">
                <%= devise_error_messages! %>
              </div>
            <% end %>

            <div class="form-group">
              <label for="username">Username:</label>

              <div class="input-group">
                <span class="input-group-addon">
                  <i class="glyphicon glyphicon-user" aria-hidden="true"></i>
                </span>

                <%= f.text_field :username, autofocus: true, class: 'form-control', maxlength: 50 %>
              </div>
            </div>

            <div class="form-group">
              <label for="email">Email:</label>

              <div class="input-group">
                <span class="input-group-addon">
                  <i class="glyphicon glyphicon-envelope" aria-hidden="true"></i>
                </span>

                <%= f.email_field :email, class: 'form-control' %>
              </div>
            </div>

            <div class="form-group switcher">
              <div class="material-switch m-r-xs">
                <%= f.check_box :show_email, id: 'switchSuccess' %>
                <label for="switchSuccess" class="label-primary"></label>
              </div>

              <label class="switcher-label">Show my email in the info card</label>
            </div>

            <div class="form-group">
              <label for="about_me">About me:</label>

              <div class="input-group">
                <span class="input-group-addon">
                  <i class="glyphicon glyphicon-info-sign" aria-hidden="true"></i>
                </span>

                <%= f.text_area :about_me, class: 'form-control vertical', maxlength: 500 %>
              </div>
            </div>

            <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
              <div class="alert alert-info m-b-md" role="alert">
                Currently waiting confirmation for: <%= resource.unconfirmed_email %>
              </div>
            <% end %>

            <div class="form-group">
              <label for="password">
                Password:
                <% if @minimum_password_length %>
                  <em>(<%= @minimum_password_length %> characters minimum)</em>
                <% end %>
              </label>

              <div class="input-group">
                <span class="input-group-addon">
                  <i class="glyphicon glyphicon-lock" aria-hidden="true"></i>
                </span>

                <%= f.password_field :password, autocomplete: 'off', class: 'form-control' %>
              </div>

              <span class="text-muted small">
                <em>leave blank if you don't want to change it</em>
              </span>
            </div>

            <div class="form-group">
              <label for="password_confirmation">Confirm password:</label>

              <div class="input-group">
                <span class="input-group-addon">
                  <i class="glyphicon glyphicon-lock" aria-hidden="true"></i>
                </span>

                <%= f.password_field :password_confirmation, autocomplete: 'off', class: 'form-control' %>
              </div>
            </div>

            <div class="form-group">
              <label for="password_confirmation">
                Current password: <em>(we need your current password to confirm your changes)</em>
              </label>

              <div class="input-group">
                <span class="input-group-addon">
                  <i class="glyphicon glyphicon-lock" aria-hidden="true"></i>
                </span>

                <%= f.password_field :current_password, autocomplete: 'off', class: 'form-control' %>
              </div>
            </div>

            <div class="actions">
              <div class="row">
                <div class="col-lg-6 col-md-6 col-sm-6 col-xs-6">
                  <%= f.submit 'Update', class: 'btn btn-default pull-left' %>
                </div>

                <div class="col-lg-6 col-md-6 col-sm-6 col-xs-6 my-card">
                  <%= content_tag :a, role: 'button', tabindex: 0, title: "About #{current_user.username}",
                                  data: {toggle: 'popover', trigger: 'hover focus', placement: 'top', html: true,
                                         content: "#{render partial: 'shared/user_card',
                                                            locals: {user: current_user}}"},
                                  class: 'btn btn-link pull-right' do %>
                    <i class="glyphicon glyphicon-eye-open" aria-hidden="true"></i>
                    My info card
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>

      <div class="panel panel-danger">
        <div class="panel-heading">
          <h3 class="panel-title text-center">
            Cancel my account
          </h3>
        </div>

        <div class="panel-body">
          <div class="alert alert-danger text-center m-b-md" role="alert">
            <h4>
              <i class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></i>
              Warning!
            </h4>

            <p>
              This will permanently delete your account. It can't be undone. All chat rooms and comments created by
              you will be deleted. Please be careful.
            </p>
          </div>

          <div class="row">
            <div class="col-lg-6 col-md-6 col-sm-6 col-xs-12 text-center">
              <h5>Unhappy?</h5>
            </div>

            <div class="col-lg-6 col-md-6 col-sm-6 col-xs-12 text-center">
              <%= button_to registration_path(resource_name), class: 'btn btn-danger',
                            data: {confirm: 'Are you sure?'}, method: :delete do %>
                <i class="glyphicon glyphicon-remove" aria-hidden="true"></i>
                <span>Cancel my account</span>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
